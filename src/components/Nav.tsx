const Nav = () => {
  return (
    <nav className="flex items-center justify-between flex-wrap bg-purple-500 p-6 sticky top-0">
      <div className="flex items-center flex-shrink-0 text-white mr-6">
        <span className="font-semibold text-xl tracking-tight">Todo-App</span>
      </div>
    </nav>
  );
};

export default Nav;
